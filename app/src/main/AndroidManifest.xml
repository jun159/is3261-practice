<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.edu.nus.baojun.is3261_practice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calculator" />
        <activity
            android:name=".sqlite.SQLite"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".SharedPreferencesActivity" />
        <activity android:name=".intent.IntentActivity" />
        <activity android:name=".intent.Activity1" />
        <activity android:name=".intent.Activity2" />
        <activity android:name=".staticfragment.StaticFragmentsActivity"></activity>
    </application>

</manifest>